# Landing page overrides

Extends `design-system/pepsino-lab/MASTER.md`.

## Goals

- Brand first: `PEPSINO LAB` is the hero signal
- One composition in first viewport (no dashboard clutter)
- Full-bleed atmospheric hero (gradient + lab grid)
- Soft UI Evolution glass panels below the fold
- Persian copy, RTL, Vazirmatn

## Motion

- Hero visible immediately (`initial={false}`)
- Section entrances: subtle y-only, never opacity 0 on SSR
- 2–3 intentional motions max above fold
